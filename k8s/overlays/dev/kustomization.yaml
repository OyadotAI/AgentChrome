apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: oya-browser-dev

resources:
- ../../base
- ingress.yaml
- cert.yaml

images:
- name: ghcr.io/oyadotia/oya-browser-server
  newTag: dev

labels:
  - pairs:
      app.kubernetes.io/version: dev
    includeSelectors: false
